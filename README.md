# Gaussian CDF calculator Project


Try running some of the following tasks:
```shell
npm install
npx hardhat node
npx hardhat run scripts/simulate.js --network hardhat
npx hardhat run scripts/run.js --network hardhat
```
